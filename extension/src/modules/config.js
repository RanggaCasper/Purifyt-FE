const PurifytConfig = Object.freeze({
  API_BASE: 'http://localhost:9000', // Set by webpack define plugin; can be overridden by user settings
  WEBSITE_URL: 'http://localhost:3000', // Set by webpack define plugin; used for links in popup
  VERSION: '1.0.0' // Set by webpack define plugin; used in popup and for update checks
})
